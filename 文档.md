# Android 对称加密与非对称加密

## 核心区别

| 特性 | 对称加密 | 非对称加密 |
|------|----------|------------|
| 密钥 | 一把密钥 | 公钥 + 私钥 |
| 速度 | 快 | 慢 |
| 典型算法 | AES | RSA |
| 密钥分发 | 困难 | 简单 |

---

## 口诀

> **对称加密：** 一把钥匙，两头用；加密快，怕泄露
>
> **非对称加密：** 公钥锁，私钥开；加密慢，分发易
>
> **混合加密：** 非对称送钥匙，对称加密数据

---

## 对称加密（Symmetric Encryption）

### 原理
加解密双方使用**同一把密钥**。

### 优点
- 加密解密**速度快**
- 适合加密**大量数据**（视频、文件、聊天消息）

### 缺点
- **密钥分发困难** - 如何安全地把密钥交给对方？

### 常见算法
- **AES** - 目前最常用
- DES - 已淘汰

### 比喻
就像一把**物理钥匙**和**保险箱**。我用这把钥匙锁上保险箱，然后把钥匙和保险箱一起寄给你。你用同一把钥匙打开。风险是快递员可能复制钥匙。

---

## 非对称加密（Asymmetric Encryption）

### 原理
生成**一对密钥**：公钥和私钥。
- **公钥** - 公开给所有人
- **私钥** - 自己保管，绝不泄露

### 特性
- **公钥锁，私钥开** - 公钥加密的数据，只有私钥能解密
- **私钥签，公钥验** - 私钥签名，公钥验证身份

### 优点
- **解决密钥分发问题** - 公钥随便发不怕泄露

### 缺点
- **加密速度极慢** - 比对称加密慢 100-1000 倍
- 不适合加密大量数据

### 常见算法
- **RSA**
- ECC

### 比喻
就像一个**安全信箱**。公钥是投递口，公开给大家；私钥是开锁钥匙，只有主人有。大家把信塞进投递口，只有主人能打开。

---

## 现实应用：混合加密

**这就是 HTTPS 的核心原理！**

### 流程

**第一步：送钥匙（非对称）**
1. 客户端问服务器：我要安全通信
2. 服务器返回公钥
3. 客户端用公钥加密一个**随机生成的对称密钥**，发送给服务器
4. 服务器用私钥解密，得到对称密钥

**第二步：加密数据（对称）**
- 双方都用这个对称密钥加密后续所有通信数据

### 为什么这么设计？
- 用非对称加密的**安全性**解决密钥分发问题
- 用对称加密的**速度**处理海量数据

---

## 面试回答模板

> 面试官好，对称加密和非对称加密的核心区别在于使用一把钥匙还是一对钥匙。

> 对称加密，我记的口诀是"一把钥匙，两头用"，比如 AES。它加解密都用同一把密钥，优点是速度快，适合加密大数据；缺点是密钥分发困难，怕中途泄露。

> 非对称加密，我记的口诀是"公钥锁，私钥开"，比如 RSA。它有公钥和私钥，公钥公开解决了分发问题，但加密速度很慢。

> 所以在实际应用中，比如 HTTPS，我们都是混合使用的。口诀是"非对称送钥匙，对称加密数据"。先用非对称加密安全传递对称密钥，然后用对称密钥加密真实数据，兼顾安全和效率。

---

## 录音内容

> 面试官你好，今天我来介绍一下 中的对称加密和非 Android对称加密。

> 它们最核心的区别在于使用一把钥匙还是一对钥匙。

> 对称加密呢，我记了个口诀，叫做"一把钥匙，两头用；加密快，怕泄露"。它的加解密都使用同一把密钥，典型代表是 AES。它最大的优点是速度快，适合加密大量数据，比如视频、文件、聊天记录。但缺点是这把钥匙的分发很困难，你在网络上传递密钥的时候，如果被黑客截获了，那所有的加密就白费了。

> 非对称加密呢，口诀是"公钥锁，私钥开；加密慢，分发易"。它会生成一对钥匙，公钥是公开的，任何人都可以拿到，私钥呢自己保管。用公钥加密的数据，只有私钥能解开。它完美解决了密钥分发的问题，但缺点是加密速度特别慢，比对称加密慢几百上千倍，所以不适合加密大量数据。

> 那在实际应用中怎么做呢？比如我们最熟悉的 HTTPS，它采用的是混合加密。我记的口诀是"非对称送钥匙，对称加密数据"。

> 具体流程是这样的：第一步，客户端问服务器要公钥，服务器把公钥给客户端。第二步，客户端自己生成一个随机的对称密钥，然后用服务器的公钥把这个对称密钥加密，发给服务器。第三步，服务器用私钥解密，得到这个对称密钥。

> 到了这一步，双方都拥有了同一把对称密钥，而且这个传递过程是安全的。接下来所有的真实数据，比如 HTTP 请求体、响应体，全部用这把对称密钥来加密传输。这样就兼顾了安全性和效率。

> 我的回答完毕，谢谢面试官。
